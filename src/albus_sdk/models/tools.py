"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .terminaltool import TerminalTool, TerminalToolTypedDict
from .websearchtool import WebSearchTool, WebSearchToolTypedDict
from albus_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class ToolsTypedDict(TypedDict):
    r"""The built-in tools the model may call. Include a tool's block to offer it (e.g. {\"web_search\": {}}); omit it to withhold it."""

    web_search: NotRequired[WebSearchToolTypedDict]
    r"""Offers the model web search."""
    terminal: NotRequired[TerminalToolTypedDict]
    r"""Offers the model a persistent Linux sandbox to run commands in.

    """


class Tools(BaseModel):
    r"""The built-in tools the model may call. Include a tool's block to offer it (e.g. {\"web_search\": {}}); omit it to withhold it."""

    web_search: Optional[WebSearchTool] = None
    r"""Offers the model web search."""

    terminal: Optional[TerminalTool] = None
    r"""Offers the model a persistent Linux sandbox to run commands in.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["web_search", "terminal"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
