"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from albus_sdk.types import BaseModel, UnrecognizedStr
from datetime import datetime
from typing import Any, Dict, Literal, Union
from typing_extensions import TypedDict


Type = Union[
    Literal[
        "llm_call",
        "tool_result",
        "harness_exit",
        "run_failed",
        "run_succeeded",
    ],
    UnrecognizedStr,
]
r"""The kind of event (e.g. \"llm_call\" for a model call and the tool calls it requested, \"tool_result\" for a tool's output).

"""


class AuditEventTypedDict(TypedDict):
    id: str
    r"""Stable identifier of this audit event within the session."""
    session_id: str
    r"""The session this event belongs to."""
    invocation_id: str
    r"""The invocation (run) during which this event occurred."""
    type: Type
    r"""The kind of event (e.g. \"llm_call\" for a model call and the tool calls it requested, \"tool_result\" for a tool's output).

    """
    payload: Dict[str, Any]
    r"""The event's details, whose shape depends on `type` (e.g. the model content and requested tool calls for \"llm_call\").

    """
    event_time: datetime
    r"""When the event occurred."""


class AuditEvent(BaseModel):
    id: str
    r"""Stable identifier of this audit event within the session."""

    session_id: str
    r"""The session this event belongs to."""

    invocation_id: str
    r"""The invocation (run) during which this event occurred."""

    type: Type
    r"""The kind of event (e.g. \"llm_call\" for a model call and the tool calls it requested, \"tool_result\" for a tool's output).

    """

    payload: Dict[str, Any]
    r"""The event's details, whose shape depends on `type` (e.g. the model content and requested tool calls for \"llm_call\").

    """

    event_time: datetime
    r"""When the event occurred."""
