"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from albus_sdk.types import BaseModel, UNSET_SENTINEL, UnrecognizedStr
from datetime import datetime
from pydantic import model_serializer
from typing import Literal, Optional, Union
from typing_extensions import NotRequired, TypedDict


MemoryStatus = Union[
    Literal[
        "active",
        "invalidated",
    ],
    UnrecognizedStr,
]
r"""`active` while agents read this memory, `invalidated` once a later memory replaced it.

"""


class MemoryTypedDict(TypedDict):
    id: str
    r"""Identifier of this memory; pass it to `DELETE /memorygroups/{group}/memories/{id}`.

    """
    content: str
    r"""The fact remembered, as the agent stated it."""
    status: MemoryStatus
    r"""`active` while agents read this memory, `invalidated` once a later memory replaced it.

    """
    created_at: datetime
    r"""When the memory was remembered."""
    source_session: NotRequired[str]
    r"""Customer-provided session identifier that produced the memory.

    """
    source_invocation: NotRequired[str]
    r"""Invocation identifier that produced the memory."""


class Memory(BaseModel):
    id: str
    r"""Identifier of this memory; pass it to `DELETE /memorygroups/{group}/memories/{id}`.

    """

    content: str
    r"""The fact remembered, as the agent stated it."""

    status: MemoryStatus
    r"""`active` while agents read this memory, `invalidated` once a later memory replaced it.

    """

    created_at: datetime
    r"""When the memory was remembered."""

    source_session: Optional[str] = None
    r"""Customer-provided session identifier that produced the memory.

    """

    source_invocation: Optional[str] = None
    r"""Invocation identifier that produced the memory."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["source_session", "source_invocation"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
