# Attempts

Attempts whose spans to return. `final` returns only the latest; `all` includes earlier spans marked `superseded`.


## Example Usage

```python
from albus_sdk.operations import Attempts
value: Attempts = "final"
```


## Values

- `"final"`
- `"all"`
